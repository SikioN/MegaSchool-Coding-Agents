# MegaSchool Coding Agents 🤖

**MegaSchool Coding Agents** — это автономная система AI-разработчиков, которая интегрируется в ваш GitHub репозиторий. Она состоит из двух агентов:
1.  **Code Agent**: Пишет код, решает задачи и создает Pull Requests.
2.  **Reviewer Agent**: Проводит Code Review, ищет ошибки и уязвимости.

Система работает через **GitHub App**, реагируя на события (новые задачи, комментарии, PR) в реальном времени.

---

## 🚀 Возможности

*   **Автоматическое написание кода**: Просто создайте Issue с описанием задачи, и агент напишет решение.
*   **Интерактивное исправление**: Напишите комментарий `/fix исправь то-то`, и агент внесет правки.
*   **Code Review**: Каждое изменение автоматически проверяется AI-ревьюером.
*   **Безопасность**: Запуск кода происходит в изолированном окружении.

---

## 🛠 Установка

Чтобы развернуть систему у себя, выполните следующие шаги:

### 1. Создание GitHub App
Для работы агентов необходимо зарегистрировать приложение в GitHub:

1.  Перейдите в [Developer Settings -> GitHub Apps -> New GitHub App](https://github.com/settings/apps/new).
2.  **Name**: Придумайте имя (напр. `My-AI-Coder`).
3.  **Webhook URL**: Укажите адрес вашего сервера (см. раздел Запуск сервера).
4.  **Permissions**:
    *   *Repository*: Contents (RW), Issues (RW), Pull requests (RW).
5.  **Subscribe to events**:
    *   Issues, Issue comment, Pull request.
6.  После создания скачайте **Private Key** (`.pem` файл).
7.  В меню "Install App" установите приложение в нужный репозиторий.

*Подробная инструкция со скриншотами доступна в файле [docs/github_app_setup.md](docs/github_app_setup.md).*

### 2. Настройка Сервера

Клонируйте репозиторий и настройте переменные окружения:

```bash
git clone https://github.com/SikioN/MegaSchool-Coding-Agents
cd MegaSchool-Coding-Agents
cp .env.example .env
```

Отредактируйте файл `.env`:

```ini
# Ключи LLM (YandexGPT или OpenAI)
LLM_API_KEY=your_api_key
YC_FOLDER_ID=your_folder_id (для Yandex)

# Настройки GitHub App
GITHUB_APP_ID=12345                 # App ID из настроек GitHub
GITHUB_WEBHOOK_SECRET=your_secret   # Webhook Secret, который вы придумали
GITHUB_PRIVATE_KEY="-----BEGIN RSA PRIVATE KEY... (содержимое .pem файла)"
```

### 3. Запуск

Установите зависимости и запустите сервер:

```bash
poetry install
poetry run uvicorn src.app:app --host 0.0.0.0 --port 8000
```

Для локальной разработки используйте `ngrok` или `localhost.run`, чтобы пробросить порт 8000 в интернет и получить публичный URL для Webhook.

---

## 🎮 Использование

Как только сервер запущен:

1.  Перейдите в свой репозиторий на GitHub.
2.  Создайте новую **Issue**.
    *   Опишите задачу (например: "Создай змейку на Python").
    *   Добавьте лейбл `ready-to-code`.
3.  **Готово!** Агент автоматически увидит задачу, напишет код и пришлет Pull Request.

Если в Pull Request ревьюер (или вы) найдет ошибку, просто напишите комментарий:
`/fix здесь ошибка, исправь...` — агент переделает код.

---

## 📞 Контакты

Автор: Муравья Никита Романович
Telegram: [t.me/nmuravya](https://t.me/nmuravya)
