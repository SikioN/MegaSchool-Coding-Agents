# MegaSchool Coding Agents 🤖

Добро пожаловать в проект **MegaSchool Coding Agents**. Это система автономных AI-разработчиков, к которой вы можете подключить свой репозиторий.

**Что умеет система:**
1.  **Code Agent**: Пишет код по вашему описанию в Issue.
2.  **Reviewer Agent**: Проверяет каждый Pull Request и ищет ошибки.
3.  **Fixer Loop**: Исправляет код по вашим комментариям (команда `/fix`).

---

## 🚀 Быстрый старт (GitHub Actions)

Самый простой способ попробовать агентов — использовать GitHub Actions. Вам не нужен свой сервер, всё работает в облаке GitHub.

### 1. Подключение к вашему репозиторию
1.  Скопируйте содержимое этого репозитория к себе (или сделайте Fork).
2.  Перейдите в **Settings -> Secrets and variables -> Actions**.
3.  Нажмите **New repository secret** и добавьте ключи для вашей LLM (Яндекс или OpenAI):

| Имя секрета | Описание | Пример значения |
|---|---|---|
| `LLM_API_KEY` | Ваш API ключ | `t1.9eu...` или `sk-...` |
| `YC_FOLDER_ID` | ID папки (для Яндекс) | `b1g...` |
| `LLM_MODEL` | (Опционально) Модель | `yandexgpt/latest` или `gpt-4` |

*(Примечание: Если вы не добавите ключи, система попробует использовать встроенные демо-ключи, но они могут иметь лимиты).*

### 2. Как пользоваться
1.  Создайте **Issue** в репозитории.
    *   Заголовок: Что сделать (напр. "Добавить функцию факториала").
    *   Лейбл: Выберите **`ready-to-code`**.
2.  Подождите пару минут. Code Agent напишет код и создаст **Pull Request**.
3.  Зайдите в PR. Reviewer Agent оставит комментарий.
4.  Если нужно что-то поправить, напишите в комментарии: `/fix исправь название функции`.

---

## 🌟 Продвинутый уровень (GitHub App)

Если вы хотите, чтобы агент реагировал **мгновенно** (через Webhooks) и работал как настоящее приложение (Gold Standard), вы можете развернуть собственное GitHub App.

### 1. Создание приложения
Подробная инструкция по регистрации приложения находится здесь: [docs/github_app_setup.md](docs/github_app_setup.md).
*(Вкратце: Создать App в настройках GitHub, получить App ID и Private Key).*

### 2. Запуск сервера
Вам понадобится сервер или всегда включенный компьютер.

1.  Клонируйте репозиторий и установите [Poetry](https://python-poetry.org/).
2.  Создайте файл `.env` и впишите туда свои ключи:
    ```ini
    # Ваши ключи GitHub App
    GITHUB_APP_ID=12345
    GITHUB_WEBHOOK_SECRET=my_secret
    GITHUB_PRIVATE_KEY="-----BEGIN RSA..."

    # Ваши ключи LLM
    LLM_API_KEY=...
    YC_FOLDER_ID=...
    ```
3.  Запустите сервер:
    ```bash
    poetry install
    poetry run uvicorn src.app:app --host 0.0.0.0 --port 8000
    ```
4.  Используйте `ngrok` или IP вашего сервера, чтобы прописать Webhook URL в настройках GitHub App.

После этого ваш личный "Coding Agent" будет работать 24/7!

---

## ⚙️ Конфигурация (Своя LLM)

Проект поддерживает любую LLM, совместимую с OpenAI API (включая YandexGPT через адаптер).
Вы можете управлять моделью через переменные окружения (или GitHub Secrets):

*   `LLM_BASE_URL`: Адрес API (например `https://api.openai.com/v1`).
*   `LLM_MODEL`: Имя модели (например `gpt-4o`, `yandexgpt-lite`).
*   `MAX_ITERATIONS`: Максимальное число попыток исправления ошибок (по умолчанию: 5).

---

## 📞 Контакты

Автор: Муравья Никита Романович
Telegram: [t.me/nmuravya](https://t.me/nmuravya)

*Проект разработан в рамках соревнования MegaSchool.*
