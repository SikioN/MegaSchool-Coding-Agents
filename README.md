# MegaSchool AI: Automated Coding Agents 🤖

Добро пожаловать в проект **MegaSchool Coding Agents**. Это система автоматизированной разработки программного обеспечения, которая использует LLM (Large Language Models) для выполнения задач разработчика и ревьюера в экосистеме GitHub.

Система состоит из двух автономных агентов:
1.  **Code Agent**: Пишет код, исправляет баги, создает Pull Requests.
2.  **Reviewer Agent**: Проверяет код, пишет замечания и блокирует/одобряет PR.

---

## 🚀 Как это работает

Процесс полностью автоматизирован через **GitHub Actions**:

1.  **Постановка задачи**: Вы создаете Issue с лейблом `ready-to-code`.
2.  **Разработка**: `Code Agent` анализирует задачу, пишет код, создает ветку и открывает Pull Request.
3.  **Ревью**: `Reviewer Agent` автоматически проверяет PR.
    *   Если всё ОК → Ставит `APPROVE`.
    *   Если есть замечания → Пишет комментарий `[REQUEST_CHANGES]`.
4.  **Исправление** (Loop): Если нужны правки, вы (или агент) пишете в комментариях к PR команду `/fix`. Агент читает замечания и исправляет код.

---

## 🛠 Установка и Запуск

Вы можете запустить проект локально или развернуть в своем репозитории.

### Предварительные требования
*   Python 3.11+
*   Poetry (менеджер зависимостей)
*   Docker (опционально)
*   Аккаунт GitHub и Yandex Cloud (или OpenAI)

### 1. Локальный запуск

1.  **Клонируйте репозиторий**:
    ```bash
    git clone https://github.com/SikioN/MegaSchool-Coding-Agents
    cd MegaSchool-AI
    ```

2.  **Установите зависимости**:
    ```bash
    poetry install
    ```

3.  **Настройте окружение**:
    Создайте файл `.env` в корне проекта и добавьте ключи (см. пример ниже).

    ```ini
    # .env
    GITHUB_TOKEN=ghp_...                # Токен с правами 'repo'
    LLM_API_KEY=...                     # API Key (YandexGPT / OpenAI)
    YC_FOLDER_ID=...                    # Folder ID (для Yandex)
    LLM_BASE_URL=https://llm.api.cloud.yandex.net/foundationModels/v1
    LLM_MODEL=yandexgpt/latest
    ```

4.  **Запустите агента вручную** (для тестирования):
    ```bash
    # Запуск Code Agent для задачи
    poetry run python -m src.main code --issue https://github.com/USER/REPO/issues/1

    # Запуск Reviewer Agent для PR
    poetry run python -m src.main review --pr https://github.com/USER/REPO/pull/2 --issue https://github.com/USER/REPO/issues/1
    ```

### 2. Запуск через GitHub Actions (Рекомендуемый)

Чтобы система работала автоматически в вашем репозитории:

1.  Запушьте этот код в свой репозиторий.
2.  Перейдите в **Settings -> Secrets and variables -> Actions**.
3.  Добавьте следующие секреты (`Repository secrets`):
    *   `LLM_API_KEY`
    *   `YC_FOLDER_ID`
    *   `LLM_BASE_URL`
    *   `LLM_MODEL`
    *   *(GITHUB_TOKEN добавлять не нужно, он встроен в Actions)*.

Теперь всё готово! Создайте Issue с лейблом `ready-to-code` и наблюдайте за магией.

### 3. Запуск через GitHub App (Gold Standard) 🌟
Самый продвинутый и быстрый способ интеграции через Webhooks.

1.  **Создайте GitHub App**:
    *   Следуйте инструкции в [docs/github_app_setup.md](docs/github_app_setup.md).
    *   Установите **Webhook URL** на ваш сервер (или ngrok).

2.  **Настройте сервер**:
    *   Добавьте переменные в `.env`:
        ```ini
        GITHUB_APP_ID=12345
        GITHUB_WEBHOOK_SECRET=your_secret
        GITHUB_PRIVATE_KEY="-----BEGIN RSA..."
        ```
    *   Запустите сервер:
        ```bash
        poetry run uvicorn src.app:app --host 0.0.0.0 --port 8000
        ```
    *   Убедитесь, что ваш `Webhook URL` доступен из интернета (используйте `ngrok` или `localhost.run`).

Теперь агент будет реагировать на события (Issue, PR, Comments) мгновенно!

---

## 📂 Структура Проекта

*   `src/agents/`: Логика агентов (`code_agent.py`, `reviewer_agent.py`).
*   `src/core/`: Ядро системы (работа с Git, Config, LLM провайдеры).
*   `.github/workflows/`: Сценарии автоматизации.
*   `Dockerfile`: Контейнеризация проекта.

---

## 🛡 Безопасность

*   **Ключи**: Все ключи хранятся в `.env` (локально) или в GitHub Secrets (удаленно). Код автоматически игнорирует файлы с секретами.
*   **Изоляция**: Агенты работают только в контексте предоставленного репозитория.

---

## 🤝 Контрибьютин

Проект разработан в рамках MegaSchool AI Track.
Автор: Муравья Никита Романович
*   Telegram: [t.me/nmuravya](https://t.me/nmuravya)
